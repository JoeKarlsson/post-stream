# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/poststream_development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Auth0 Configuration (if using Auth0)
# Note: CLIENT_SECRET and TOKEN should only be used server-side
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_TOKEN=your-auth0-token
AUTH0_DOMAIN=your-auth0-domain
AUTH0_CALLBACK_URL=http://localhost:3000/callback

# Server Configuration
NODE_ENV=development
PORT=3001

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
